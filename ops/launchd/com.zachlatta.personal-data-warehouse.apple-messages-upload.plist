<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>Label</key>
    <string>com.zachlatta.personal-data-warehouse.apple-messages-upload</string>

    <key>ProgramArguments</key>
    <array>
      <string>/Users/zrl/dev/zachlatta/personal-data-warehouse/bin/apple-messages-upload-launchd</string>
    </array>

    <key>StartInterval</key>
    <integer>300</integer>

    <!-- Fire on change too: the SQLite WAL is what the app writes on every
         new message, so a message is uploaded seconds after it arrives instead
         of at the next five-minute tick. ThrottleInterval bounds a chatty WAL
         to one run a minute; StartInterval stays as the floor. -->
    <key>WatchPaths</key>
    <array>
      <string>/Users/zrl/Library/Messages/chat.db-wal</string>
    </array>

    <key>ThrottleInterval</key>
    <integer>60</integer>

    <key>RunAtLoad</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/Users/zrl/Library/Logs/personal-data-warehouse/apple-messages-upload.launchd.out.log</string>

    <key>StandardErrorPath</key>
    <string>/Users/zrl/Library/Logs/personal-data-warehouse/apple-messages-upload.launchd.err.log</string>
  </dict>
</plist>
