{
  "additionalProperties": false,
  "properties": {
    "format": {
      "description": "result format: csv, json, or ndjson; default csv",
      "type": "string"
    },
    "queries": {
      "description": "array of query objects; each must include question and sql",
      "items": {
        "additionalProperties": false,
        "properties": {
          "question": {
            "description": "concise plain-English question this SQL statement is trying to answer",
            "type": "string"
          },
          "sql": {
            "description": "read-only Postgres SQL string to run",
            "type": "string"
          }
        },
        "required": [
          "question",
          "sql"
        ],
        "type": "object"
      },
      "type": [
        "null",
        "array"
      ]
    }
  },
  "required": [
    "queries"
  ],
  "type": "object"
}
